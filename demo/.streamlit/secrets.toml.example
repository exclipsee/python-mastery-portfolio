# Copy this file to demo/.streamlit/secrets.toml or to %USERPROFILE%/.streamlit/secrets.toml
# and adjust the API_URL to point to your running FastAPI service.
# The Streamlit app will also accept the API_URL environment variable and defaults to http://localhost:8000.

API_URL = "http://localhost:8000"
